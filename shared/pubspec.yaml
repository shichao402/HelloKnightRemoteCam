name: shared
description: Shared code for HelloKnightRemoteCam client and server
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Archive handling
  archive: ^3.4.0
  
  # Crypto for hash verification
  crypto: ^3.0.3
  
  # HTTP client for update checking
  dio: ^5.4.0
  
  # Path manipulation
  path: ^1.8.3
  
  # YAML parsing
  yaml: ^3.1.2
  
  # Platform detection
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

